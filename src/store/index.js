import { createStore } from "vuex";

const store = createStore({
  state: {
    // Burada Vuex Store'unuzun durumunu (state) tanımlayın
  },
  mutations: {
    // Burada mutasyonları tanımlayın
  },
  actions: {
    // Burada eylemleri tanımlayın
  },
  getters: {
    // Burada getter'ları tanımlayın
  },
});

export default store;
